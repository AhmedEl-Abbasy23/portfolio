/*
import 'package:hr_plus/generated/assets.dart';
import 'package:flutter/material.dart';

class CircleLogoWidget extends StatelessWidget {
  const CircleLogoWidget({super.key, this.width});

  final double? width;

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      Assets.appCircleLogo,
      width: width ?? 100,
    );
  }
}

class LogoWidget extends StatelessWidget {
  const LogoWidget({super.key, this.width, this.height});

  final double? width;
  final double? height;

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      Assets.appLogo,
      height: height ?? 53,
      width: width ?? 175,
    );
  }
}
*/
